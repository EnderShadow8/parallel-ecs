{
  "name": "parallel-ecs",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/*.ts --outdir=dist/ --target=esnext --platform=node --format=esm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.14.22",
    "typescript": "^4.6.2"
  },
  "type": "module",
  "dependencies": {
    "worker-rwlock": "^0.1.0"
  }
}
